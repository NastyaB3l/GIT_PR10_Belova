name: MyAction 
on: workflow_dispatch   
jobs:
    NameJob:
        runs-on: windows-latest    
        steps:
            - name: Checkout
              uses: actions/checkout@v5.0.0
            - name: Run application
              run: dotnet run --project ./PR10_Belova.csproj